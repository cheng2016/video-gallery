---
description: Git commit 说明必须用中文并详细写清改动
alwaysApply: true
---

# Commit 说明规范

创建 git commit 时：

1. **必须用中文**写 commit message（标题与正文均可中文）。
2. **详细说明本次改动**：不要只用笼统短语；写清改了什么、为什么改、影响哪些行为。
3. 推荐结构：
   - 第一行：一句话概括（动词开头，如「修复」「新增」「重构」）
   - 空一行后：分点列出主要变更与原因
4. 仅在用户明确要求 commit 时才提交；提交前仍按仓库既有 git 安全协议执行。
